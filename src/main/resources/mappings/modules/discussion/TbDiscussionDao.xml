<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.discussion.dao.TbDiscussionDao">
    
	<sql id="tbDiscussionColumns">
		a.id AS "id",
		a.self_id AS "selfId",
		a.point_to_id AS "pointToId",
		a.reply_content AS "replyContent",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag"
	</sql>
	
	<sql id="tbDiscussionJoins">
	</sql>
    
	<select id="get" resultType="TbDiscussion">
		SELECT 
			<include refid="tbDiscussionColumns"/>
		FROM tb_discussion a
		<include refid="tbDiscussionJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="TbDiscussion">
		SELECT 
			<include refid="tbDiscussionColumns"/>
		FROM tb_discussion a
		<include refid="tbDiscussionJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="TbDiscussion">
		SELECT 
			<include refid="tbDiscussionColumns"/>
		FROM tb_discussion a
		<include refid="tbDiscussionJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO tb_discussion(
			id,
			self_id,
			point_to_id,
			reply_content,
			create_by,
			create_date,
			update_by,
			update_date,
			remarks,
			del_flag
		) VALUES (
			#{id},
			#{selfId},
			#{pointToId},
			#{replyContent},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{remarks},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE tb_discussion SET 	
			self_id = #{selfId},
			point_to_id = #{pointToId},
			reply_content = #{replyContent},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE tb_discussion SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>