<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.file.dao.TbFileDao">
    
	<sql id="tbFileColumns">
		a.id AS "id",
		a.file_title AS "fileTitle",
		a.file_iden AS "fileIden",
		a.file_path AS "filePath",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag"
	</sql>
	
	<sql id="tbFileJoins">
	</sql>
    
	<select id="get" resultType="TbFile">
		SELECT 
			<include refid="tbFileColumns"/>
		FROM tb_file a
		<include refid="tbFileJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="TbFile">
		SELECT 
			<include refid="tbFileColumns"/>
		FROM tb_file a
		<include refid="tbFileJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="TbFile">
		SELECT 
			<include refid="tbFileColumns"/>
		FROM tb_file a
		<include refid="tbFileJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO tb_file(
			id,
			file_title,
			file_iden,
			file_path,
			create_by,
			create_date,
			update_by,
			update_date,
			remarks,
			del_flag
		) VALUES (
			#{id},
			#{fileTitle},
			#{fileIden},
			#{filePath},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{remarks},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE tb_file SET 	
			file_title = #{fileTitle},
			file_iden = #{fileIden},
			file_path = #{filePath},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE tb_file SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>