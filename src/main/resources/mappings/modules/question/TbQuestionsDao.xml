<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.question.dao.TbQuestionsDao">
    
	<sql id="tbQuestionsColumns">
		a.id AS "id",
		a.question_id AS "questionId",
		a.message_id AS "messageId",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag"
	</sql>
	
	<sql id="tbQuestionsJoins">
	</sql>
    
	<select id="get" resultType="TbQuestions">
		SELECT 
			<include refid="tbQuestionsColumns"/>
		FROM tb_questions a
		<include refid="tbQuestionsJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="TbQuestions">
		SELECT 
			<include refid="tbQuestionsColumns"/>
		FROM tb_questions a
		<include refid="tbQuestionsJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="TbQuestions">
		SELECT 
			<include refid="tbQuestionsColumns"/>
		FROM tb_questions a
		<include refid="tbQuestionsJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO tb_questions(
			id,
			question_id,
			message_id,
			create_by,
			create_date,
			update_by,
			update_date,
			remarks,
			del_flag
		) VALUES (
			#{id},
			#{questionId},
			#{messageId},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{remarks},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE tb_questions SET 	
			question_id = #{questionId},
			message_id = #{messageId},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE tb_questions SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>